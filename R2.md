# การย้าย media ไป Cloudflare R2

โปรเจกต์นี้ย้ายไฟล์ media หนัก (วิดีโอ + เสียง) ออกจาก repo ไปเก็บบน **Cloudflare R2**
แล้วเสิร์ฟผ่าน URL สาธารณะ โค้ดอ้างอิงผ่าน helper `mediaUrl()` ใน [src/lib/media.ts](src/lib/media.ts)

> ทำไมต้องทำ: ไฟล์วิดีโอใหญ่เกิน 100MB (push GitHub ไม่ได้) + เกิน 25MB (Cloudflare Worker เสิร์ฟไม่ได้) + ทำให้ build ใน Docker OOM

---

## ภาพรวมการทำงาน

```
<video src={mediaUrl("NanshortFilm/หนัง.mp4")} />
        │
        └─> VITE_R2_BASE + "/NanshortFilm/หนัง.mp4"  (encode ภาษาไทยให้อัตโนมัติ)
            = https://media.nanfest.com/NanshortFilm/หนัง.mp4
```

**object key บน R2 = path ที่สัมพันธ์กับ `src/assets/`** เช่นไฟล์ `src/assets/Nanmusic/ไทลื้อ Master.wav`
จะมี key เป็น `Nanmusic/ไทลื้อ Master.wav`

---

## ขั้นตอนที่ 1 — สร้าง R2 Bucket

1. เข้า Cloudflare Dashboard → **R2** → **Create bucket**
2. ตั้งชื่อ เช่น `nanfest-media` → Create
   (ถ้าใช้ชื่ออื่น ตอนรันสคริปต์ให้ส่ง `BUCKET=ชื่อ` ด้วย)

## ขั้นตอนที่ 2 — เปิดให้เข้าถึงแบบ public

เลือกอย่างใดอย่างหนึ่ง:

**ก) Custom domain (แนะนำ)**
- ใน bucket → **Settings** → **Public access** → **Connect Domain**
- ใส่ subdomain เช่น `media.nanfest.com` (โดเมนต้องอยู่ใน Cloudflare)
- ได้ URL: `https://media.nanfest.com`

**ข) r2.dev (เร็ว ทดสอบง่าย)**
- ใน bucket → **Settings** → **Public access** → เปิด **Allow Access** ของ r2.dev
- ได้ URL หน้าตา: `https://pub-xxxxxxxxxxxx.r2.dev`

## ขั้นตอนที่ 3 — ตั้งค่า .env

แก้ไฟล์ [.env](.env) ให้ `VITE_R2_BASE` เป็น URL จากขั้นตอนที่ 2:

```bash
VITE_R2_BASE=https://media.nanfest.com
```

> `.env` ถูก gitignore แล้ว — ไม่ขึ้น git ใช้ [.env.example](.env.example) เป็นแม่แบบ

## ขั้นตอนที่ 4 — อัปโหลดไฟล์ขึ้น R2

ติดตั้งและ login wrangler ก่อน:

```bash
npm i -g wrangler
wrangler login
```

**บีบวิดีโอก่อน** (ครั้งเดียว — ผลอยู่ใน `dist-media/`):

```bash
./scripts/compress-videos.sh
```

แล้วรันสคริปต์อัปโหลด (วิดีโออัปจาก `dist-media/` ที่บีบแล้ว, เสียง+รูปจาก `src/assets/`):

```bash
BUCKET=nanfest-media ./scripts/upload-media-to-r2.sh
```

### ทางเลือก: อัปทีละไฟล์ด้วยมือ
```bash
wrangler r2 object put nanfest-media/NanshowD3/Nanfest\ Showcase.mp4 \
  --file="src/assets/NanshowD3/Nanfest Showcase.mp4" --remote
```

### ทางเลือก: อัปจำนวนมากด้วย rclone (เร็วกว่า ทำขนานได้)
ตั้งค่า rclone remote ชนิด S3 ชี้ R2 (ใช้ R2 Access Key/Secret จาก Dashboard → R2 → Manage API Tokens) แล้ว:
```bash
rclone copy "src/assets" r2:nanfest-media \
  --include "*.mp4" --include "*.wav" --include "*.mov" -P
```

## ขั้นตอนที่ 5 — ทดสอบ

```bash
npm run dev
```
เปิดหน้า /cinema/short-film, /cinema/music, /nan2026/music, /nan2026/Showcase2026,
/festivals/showcase แล้วเช็กว่าวิดีโอ/เสียงเล่นได้ (โหลดจาก R2)

## ขั้นตอนที่ 6 — ลบ media ออกจาก repo

เมื่อยืนยันว่าเล่นจาก R2 ได้แล้ว ลบไฟล์หนักออกจาก repo ได้เลย
(`.gitignore` กัน `*.mp4 *.wav` ไว้แล้ว ไม่ให้หลุดขึ้น git):

```bash
find src/assets -type f \( -iname "*.mp4" -o -iname "*.wav" \) -delete
```

---

## สิ่งที่ย้ายไป R2 แล้ว

| ชนิด | รายละเอียด |
|---|---|
| 🎬 วิดีโอ | ทั้งหมด 11 ไฟล์ |
| 🎵 เสียง | .wav 6 เพลง |
| 🖼️ รูปของ Showcase2026 | โฟลเดอร์ `Nanfestshowcase2026`, `jaiban`, `Nanlife`, `sp` |

> รูปของ Showcase2026 เดิมอ้างด้วย path `/src/assets/...` แบบ string ซึ่ง **ใช้ได้แค่ตอน dev พอ build จริงรูปไม่ขึ้น** —
> รอบนี้แก้ให้ render ผ่าน `mediaUrl()` เรียบร้อยแล้ว (บั๊กหาย) สคริปต์อัปโหลดครอบคลุมรูปกลุ่มนี้ให้แล้ว

## รูปภาพส่วนที่เหลือ (ยังอยู่ใน repo)

รูปในหน้าอื่นๆ ใช้ **ES import** (`import x from "@/assets/..."`) ซึ่ง Vite bundle ให้ถูกต้องตอน build อยู่แล้ว
จึงยังเก็บใน repo ได้ตามปกติ ถ้าต้องการลดขนาดเพิ่ม ค่อยทยอยบีบเป็น WebP / ย้าย R2 รอบถัดไป

### ลบรูป Showcase2026 ออกจาก repo (หลังอัป R2 + ทดสอบแล้ว)
```bash
# ⚠️ Nanlife ถูก import ในหน้า /nan2026/life ด้วย — อย่าลบทั้งโฟลเดอร์
rm -rf src/assets/Nanfestshowcase2026 src/assets/jaiban src/assets/sp
# Nanlife: ลบเฉพาะไฟล์ที่ life.tsx ไม่ได้ใช้ หรือเก็บไว้ทั้งโฟลเดอร์ก็ได้
```
